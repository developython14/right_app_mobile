import 'package:flutter/material.dart';

class emoji_text extends StatelessWidget {
  const emoji_text({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        padding: EdgeInsets.only(left: 25),
        child: Text("MUSTAPHA BELKASSEM EMOJI "));
  }
}
