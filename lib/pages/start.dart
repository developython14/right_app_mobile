import 'dart:html';

import 'package:flutter/material.dart';
import 'package:right_saudi/pages/componant/body.dart';

class welecome extends StatelessWidget {
  const welecome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: body(),
    );
  }
}
